apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fabijanbajo.com.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "fabijanbajo.com.name" . }}
    helm.sh/chart: {{ include "fabijanbajo.com.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "fabijanbajo.com.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
